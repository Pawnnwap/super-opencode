"""Codex execution-agent support modules.

Mirror of ``opencode_support`` for the open-source Codex CLI
(``codex exec``). Provides cross-platform (Windows + Linux) executable
location, command building, and subprocess driving. Shared pieces
(``RunResult``, ``extract_file_refs``) are imported directly from
``opencode_support`` instead of being duplicated.
"""
